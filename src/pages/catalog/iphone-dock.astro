---
import Layout from '~/layouts/PageLayout.astro';
import { getPermalink } from '~/utils/permalinks';
import { Image } from 'astro:assets';

import img1 from '~/assets/images/Gallery/iPhone_dock/Image1.png';
import img2 from '~/assets/images/Gallery/iPhone_dock/Image2.png';
import img3 from '~/assets/images/Gallery/iPhone_dock/Image3.png';

const metadata = {
  title: 'Custom 3D Printed iPhone Standby Dock in Luxembourg — tattva-3d',
  description: 'Custom 3D printed iPhone standby dock with wireless charging in Luxembourg. Perfect for MagSafe and StandBy mode — made on demand for €20.',
};

const items = [
  { image: img1, title: 'iPhone Standby Dock', description: 'Wireless charging dock designed for iPhone StandBy mode', price: '€20' },
  { image: img2, title: 'iPhone Standby Dock', description: 'Elevated stand for perfect nightstand angle', price: '€20' },
  { image: img3, title: 'iPhone Standby Dock', description: 'Custom 3D printed with clean cable management', price: '€20' },
];
---

<Layout metadata={metadata}>
  <section class="pt-24 pb-12 bg-gradient-to-b from-primary/10 to-transparent dark:from-primary/20">
    <div class="max-w-7xl mx-auto px-4 sm:px-6">
      <nav class="mb-6 text-sm">
        <a href={getPermalink('/')} class="text-gray-600 dark:text-gray-400 hover:text-primary">Home</a>
        <span class="mx-2 text-gray-400">/</span>
        <a href={getPermalink('/catalog')} class="text-gray-600 dark:text-gray-400 hover:text-primary">Catalog</a>
        <span class="mx-2 text-gray-400">/</span>
        <span class="text-gray-900 dark:text-gray-100 font-semibold">iPhone Standby Dock</span>
      </nav>

      <div class="grid grid-cols-1 lg:grid-cols-2 gap-10 items-start">
        <div>
          <div class="flex items-center gap-4 mb-6">
            <div class="text-6xl">📱</div>
            <div>
              <h1 class="text-4xl md:text-5xl font-bold text-gray-900 dark:text-gray-100">iPhone Standby Dock</h1>
              <p class="text-xl text-gray-600 dark:text-gray-400 mt-2">3D printed dock designed for iPhone StandBy mode</p>
            </div>
          </div>

          <div class="grid grid-cols-2 gap-3 mb-6">
            <div class="flex items-center gap-2 text-gray-700 dark:text-gray-300">
              <span class="text-2xl">⚡</span><span class="text-sm font-medium">Wireless Charging</span>
            </div>
            <div class="flex items-center gap-2 text-gray-700 dark:text-gray-300">
              <span class="text-2xl">🌙</span><span class="text-sm font-medium">StandBy Mode Ready</span>
            </div>
            <div class="flex items-center gap-2 text-gray-700 dark:text-gray-300">
              <span class="text-2xl">🖨️</span><span class="text-sm font-medium">Fully 3D Printed</span>
            </div>
            <div class="flex items-center gap-2 text-gray-700 dark:text-gray-300">
              <span class="text-2xl">🎨</span><span class="text-sm font-medium">Custom Colors</span>
            </div>
          </div>

          <p class="text-lg text-gray-600 dark:text-gray-400">
            The perfect companion for iPhone's StandBy mode. This 3D printed dock holds your iPhone at the ideal landscape angle while it charges wirelessly — so you always have a bedside clock, photo frame, or smart display within sight. Designed for MagSafe compatibility and tidy cable routing.
          </p>
        </div>

        <div class="bg-white dark:bg-slate-800 rounded-2xl shadow-xl p-8 border border-gray-100 dark:border-slate-700">
          <div class="pb-6 mb-6 border-b border-gray-200 dark:border-slate-700">
            <p class="text-xs font-semibold uppercase tracking-widest text-gray-500 dark:text-gray-400 mb-4 text-center">Pricing</p>
            <div class="space-y-3">
              <div class="flex items-center justify-between px-4 py-3 bg-gray-50 dark:bg-slate-700/50 rounded-lg">
                <span class="text-sm font-medium text-gray-700 dark:text-gray-300">iPhone Standby Dock</span>
                <span class="text-2xl font-extrabold text-primary">€20</span>
              </div>
            </div>
          </div>

          <a
            href="/order?product=iphone-dock"
            target="_blank"
            rel="noopener noreferrer"
            class="block w-full text-center bg-primary hover:bg-primary-dark text-white font-bold py-4 px-8 rounded-lg transition-all duration-300 transform hover:scale-105 hover:shadow-lg mb-3"
          >
            Order Your Dock — €20
          </a>

          <p class="text-center text-sm text-gray-500 dark:text-gray-400">
            Questions? <a href="mailto:t-3d@ik.me" class="text-primary hover:underline font-medium">t-3d@ik.me</a>
          </p>
        </div>
      </div>
    </div>
  </section>

  <section class="py-16 bg-white dark:bg-slate-900">
    <div class="max-w-7xl mx-auto px-4 sm:px-6">
      <h2 class="text-3xl font-bold text-gray-900 dark:text-gray-100 mb-8">Examples</h2>

      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 mb-12">
        {items.map((item, index) => (
          <div class="group relative bg-white dark:bg-slate-800 rounded-xl overflow-hidden shadow-md hover:shadow-2xl transition-all duration-300">
            <div class="relative aspect-square bg-gray-100 dark:bg-slate-700">
              <Image
                src={item.image}
                alt={item.title}
                class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-500"
                widths={[400, 768]}
                sizes="(max-width: 768px) 100vw, 400px"
              />
              {index === 0 && item.price && (
                <div class="absolute top-3 right-3 bg-black/70 backdrop-blur-sm text-white rounded-lg px-2.5 py-1.5 text-center leading-tight pointer-events-none">
                  <div class="text-lg font-extrabold">{item.price}</div>
                </div>
              )}
            </div>
            <div class="p-4">
              <h3 class="font-bold text-lg text-gray-900 dark:text-gray-100 mb-1">{item.title}</h3>
              <p class="text-sm text-gray-600 dark:text-gray-400">{item.description}</p>
            </div>
          </div>
        ))}
      </div>

      <p class="text-xs text-gray-400 dark:text-gray-500 mb-8">For illustration only. Actual product may differ.</p>

      <div class="bg-gradient-to-r from-primary/10 to-secondary/10 dark:from-primary/20 dark:to-secondary/20 rounded-2xl p-10 text-center">
        <h3 class="text-2xl md:text-3xl font-bold text-gray-900 dark:text-gray-100 mb-4">Want a Custom iPhone Dock?</h3>
        <p class="text-lg text-gray-600 dark:text-gray-400 mb-6 max-w-2xl mx-auto">
          Choose your color, add a name or logo, or request a specific cable routing — we'll make it fit your space perfectly.
        </p>
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
          <a
            href="/order?product=iphone-dock"
            target="_blank"
            rel="noopener noreferrer"
            class="inline-flex items-center justify-center gap-2 bg-primary hover:bg-primary-dark text-white font-bold py-4 px-8 rounded-lg transition-all duration-300 transform hover:scale-105"
          >
            <span>Order Now — €20</span>
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6"/></svg>
          </a>
          <a
            href={getPermalink('/catalog')}
            class="inline-flex items-center justify-center gap-2 border-2 border-gray-300 dark:border-gray-600 hover:border-primary dark:hover:border-primary text-gray-900 dark:text-gray-100 font-bold py-4 px-8 rounded-lg transition-all duration-300"
          >
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"/></svg>
            <span>Back to Catalog</span>
          </a>
        </div>
      </div>
    </div>
  </section>
</Layout>
